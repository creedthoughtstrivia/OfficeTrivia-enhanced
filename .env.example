# Example environment file for Creed Thoughts Trivia
# Copy this file to `.env` and adjust values as needed.  These values are
# loaded by the game at runtime via `window.ADMIN_PASSCODE` and are not
# committed to the repository.

# Admin passcode used to access /admin.html.  Change this to your own secret
# string before deploying to production.
ADMIN_PASSCODE=dwight-rules-1982